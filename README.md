# Generative UI Composer

A flashy, unique UI-builder playground built with React + Vite (JavaScript) and Tailwind CSS v3.

## Milestone 1 — What this repo contains

- Vite React (JavaScript) scaffold
- Tailwind CSS v3 configured to work with Vite
- Minimal demo `App.jsx` to verify Tailwind classes

## Run locally

```bash
npm install
npm run dev
```
